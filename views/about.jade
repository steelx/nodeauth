extends layout

block content
  .row
    .small-6.large-6.columns
      h1= title
      p Welcome to #{title}

      h3 Nodejs simple session authentication

      h4 Restrict access to members page
      p In this case which is just the index page, any access to index page will get redirected to login page.

      h4 Login and Logout
      p Logged users should only see Logout button.
